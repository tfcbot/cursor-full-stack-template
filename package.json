{
  "name": "cursor-full-stack-template",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd packages/frontend dev",
    "build": "bun run --cwd packages/frontend build",
    "test": "bun test",
    "test:core": "bun run --cwd packages/core test",
    "test:frontend": "bun run --cwd packages/frontend test",
    "test:api": "bun run --cwd packages/core test:api"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "typescript": "^5"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "sst": "^3",
    "zod": "^3.24.2"
  },
  "trustedDependencies": [
    "sharp"
  ],
  "repository": "https://github.com/tfcbot/cursor-full-stack-template.git",
  "author": "tfcbot <admin@blurware.com>",
  "license": "MIT",
  "private": true
}
