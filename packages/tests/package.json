{
  "name": "tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:unit": "TEST_TYPE=unit vitest run unit.test.ts",
    "test:integration": "TEST_TYPE=integration vitest run integration.test.ts",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "@vitest/coverage-v8": "^3.1.1",
    "cross-fetch": "^4.0.0",
    "dotenv": "^16.3.1",
    "jsdom": "^26.0.0",
    "typescript": "^5.3.3",
    "vitest": "^3.1.1"
  }
}
