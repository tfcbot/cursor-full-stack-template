import { ContentList } from '../components/ContentList';

export const metadata = {
  title: 'Your Content - Content Generator',
  description: 'View all your generated content',
};

export default function ContentPage() {
  return <ContentList />;
} 